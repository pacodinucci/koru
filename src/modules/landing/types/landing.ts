export type LandingMetric = {
  label: string;
  value: string;
};

export type LandingPillar = {
  id: string;
  title: string;
  subtitle: string;
  description: string;
};

export type LandingProgram = {
  stage: string;
  studio: string;
  ages: string;
  summary: string;
};
